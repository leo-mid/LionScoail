= simple_form_for [@conversation, @message] do |f|
  .message-bar.d-flex.gap-2
    = f.input :body, label: false, as: :string, placeholder: "Send message", wrapper: false, input_html: { class: "form-control" }
    = f.submit "Send", class: "btn btn-primary"
