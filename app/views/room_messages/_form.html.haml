= simple_form_for [@room, @room_message], local: true, html: { data: { turbo: false } } do |f|
  .input-group
    = f.input :message, label: false, wrapper: false, input_html: { class: "form-control" }
    = f.submit "Send", class: "btn btn-primary"