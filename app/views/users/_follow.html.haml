= simple_form_for(current_user.active_relationships.build, local: true, html: { data: { turbo: false } }) do |f|
  = hidden_field_tag :followed_id, user.id
  = f.submit "Follow", class: "btn btn-primary"
